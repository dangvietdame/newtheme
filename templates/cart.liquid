<h2>Cart</h2>
{% form 'cart', cart %}
    {% if cart.empty? %}
        <p>Your cart is empty!</p>
    {% else %}
        {% for line_item in cart.items %}
            <div>
                <img src="{{ line_item.image | image_url: width: 200 }}"/>
                <div>{{ line_item.title }}</div>
                <div>{{ line_item.final_line_price | money }}</div>
                <input name="updates[]" value="{{ line_item.quantity }}">
                <a href="{{ line_item.url_to_remove }}">Remove</a>
            </div>
        {% endfor %}
        <hr>
        <div>Total: {{ cart.total_price | money }}</div>
        <button type="submit" name="update">Update</button>
        <button type="submit" name="checkout">Checkout</button>
    {% endif %}
{% endform %}
