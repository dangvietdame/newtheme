<div>Product Count: {{cart.item_count}}</div>
{% for item in cart.items %}
    <div>{{item.title}}</div>
    <div>{{item.quantity}}</div>
    <div>{{item.id}}</div>
    <a href="{{item.url_to_remove}}">Delete</a>
{% endfor %}